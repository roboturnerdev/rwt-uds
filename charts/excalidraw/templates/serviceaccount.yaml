{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default "excalidraw" .Values.serviceAccount.name }}
  labels:
    {{- include "excalidraw.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}